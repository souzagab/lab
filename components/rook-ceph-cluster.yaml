apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rook-ceph-cluster
  annotations:
    argocd.argoproj.io/manifest-generate-paths: /charts/rook-ceph-cluster

spec:
    project: default

    destination:
      name: ""
      server: https://kubernetes.default.svc
      namespace: rook-ceph-cluster

    source:
      helm:
        valueFiles:
          - values.yaml
      path: charts/rook-ceph-cluster
      repoURL: https://github.com/souzagab/lab
      targetRevision: main

    syncPolicy:
      automated:
        prune: true
        selfHeal: false
        allowEmpty: false
      syncOptions:
        - CreateNamespace=true
